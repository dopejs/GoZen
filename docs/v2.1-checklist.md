# v2.1 Pre-Release Checklist

## Before Release

- [ ] Audit all CLI interactive flows and terminal display
  - Verify every interactive command uses alt screen (no TUI residue in terminal)
  - Verify confirmation messages print correctly after TUI exit
  - Verify cancel (esc/ctrl-c) exits cleanly with no output
  - Commands to check: `zen config add provider`, `zen config edit provider`, `zen config add profile`, `zen config edit profile`, `zen config default-client`, `zen config default-profile`, `zen pick`, `zen` (first-run create provider)

- [ ] Fix profile add/edit interactive flow issues

- [ ] Verify completion messages for install, upgrade, and opencc-migrate
  - `zen upgrade`: confirm post-upgrade success message is clear and correct
  - Install script: confirm post-install message and next-step instructions
  - `opencc-migrate`: confirm migration completion message and any warnings
