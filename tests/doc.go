package tests

// This file exists to prevent "no Go files" build errors when running
// `go test ./...` without the "integration" build tag.
// To run integration tests: go test -tags integration -v -timeout 120s ./tests/
